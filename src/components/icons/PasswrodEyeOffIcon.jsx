import { EyeOffIcon } from 'lucide-react';

export default function PasswordEyeOffIcon({ size = 20, color = 'black', strokeWidth = 2 }) {
  return <EyeOffIcon size={size} color={color} strokeWidth={strokeWidth} />;
}
