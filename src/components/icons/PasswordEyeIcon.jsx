import { EyeIcon } from 'lucide-react';

export default function PasswordEyeIcon({ size = 20, color = 'black', strokeWidth = 2 }) {
  return <EyeIcon size={size} color={color} strokeWidth={strokeWidth} />;
}
